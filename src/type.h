#pragma once

/** !brief A custom type

This is a longer description for a custom type.

\verbatim embed:rst
An inline example for MyType.
 
.. code-block:: cpp
    MyType x;
    x.foo();
\endverbatim
*/
struct MyType {
    /** \brief Do a lot of foo */
    void foo();
};